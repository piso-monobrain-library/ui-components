{
	"devDependencies": {
		"@babel/cli": "^7.26.4",
		"@babel/core": "^7.26.7",
		"@babel/preset-env": "^7.26.7",
		"gh-pages": "^6.3.0"
	},
	"scripts": {
		"build": "babel src --out-dir dist && cp index.html dist && cp style.css dist",
		"predeploy": "npm run build",
		"deploy": "gh-pages -d dist",
		"start": "npm run predeploy && npm run deploy"
	}
}
